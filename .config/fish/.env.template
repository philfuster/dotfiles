# Fish shell environment variables template
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file with real credentials

# SSH private key path for keychain
# Path to your SSH private key file (tilde will be expanded)
# Example: ~/.ssh/id_rsa or ~/.ssh/id_ed25519
# For multiple keys: ~/.ssh/id_rsa ~/.ssh/id_ed25519
set -gx SSH_PRIVATE_KEYS your_private_key_paths_here

# SSH key passphrase for keychain
# Used by the fish_keychain function to automatically unlock SSH keys
# Replace with your actual passphrase
set -gx SSH_KEY_PASSPHRASE your_passphrase_here
